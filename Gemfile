# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem "slack-ruby-bot" #API interface to interact with bot
gem "puma" #config.ru file, start instance have running in foreground
gem "dotenv" #load environment variables that have been created
gem "celluloid-io" 
